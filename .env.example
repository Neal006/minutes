# Claude writes the notes and answers questions. https://console.anthropic.com
ANTHROPIC_API_KEY=
# CLAUDE_MODEL=claude-opus-5

# Transcription: any OpenAI-compatible /audio/transcriptions endpoint.
# OpenAI (default):
OPENAI_API_KEY=
# Groq (fast + cheap):
# STT_BASE_URL=https://api.groq.com/openai/v1
# STT_MODEL=whisper-large-v3-turbo
# STT_API_KEY=
# Local (e.g. speaches / faster-whisper-server):
# STT_BASE_URL=http://localhost:8000/v1
# STT_MODEL=Systran/faster-whisper-small

# Automations: POST notes here when a meeting is ready (comma-separated; Slack incoming webhooks work as-is)
# WEBHOOK_URLS=https://hooks.slack.com/services/...

# PORT=3001
# DATA_DIR=./data
